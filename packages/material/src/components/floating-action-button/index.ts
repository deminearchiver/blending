import { factory } from "./floating-action-button";
export const FloatingActionButton = {
  Primary: factory({
    variant: "primary"
  }),
  Secondary: factory({
    variant: "secondary"
  }),
  Tertiary: factory({
    variant: "tertiary"
  }),
  Surface: factory({
    variant: "surface"
  }),
};
