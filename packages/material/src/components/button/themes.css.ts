import { createThemeContract } from "@vanilla-extract/css";

export const elevatedButtonTheme = createThemeContract({
  
});
export const filledButtonTheme = createThemeContract({
  
});
export const tonalButtonTheme = createThemeContract({
  
});
